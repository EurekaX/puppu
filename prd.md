这份针对“狗狗驱虫与疫苗记录小程序”的产品需求文档（PRD）专为 AI 开发者（如 Claude Code）设计。文档逻辑清晰、字段明确，方便 AI 直接解析并生成代码。

---

# 产品需求文档 (PRD)：狗狗驱虫与疫苗记录小程序

## 1. 项目概述

* **产品名称**： 宠安记 (PetCare Log)
* **产品类型**： 微信小程序（微信原生开发 或 Uni-app）
* **核心目标**： 帮助宠物主人数字化管理狗狗的个人档案，精准记录驱虫（体内/体外）和疫苗接种情况，并通过智能提醒防止漏种、漏驱。
* **目标开发工具**： Claude Code

---

## 2. 页面结构与功能架构 (Information Architecture)

小程序采用**底部三标签 (Tabbar)** 结构：

```
├── 首页 (Dashboard)
│   ├── 狗狗切换/档案概览
│   └── 今日任务/近期到期提醒（疫苗、驱虫）
├── 记录 (Records)
│   ├── 疫苗记录历史（支持列表、详情、添加）
│   └── 驱虫记录历史（支持列表、详情、添加）
└── 我的 (Profile)
    ├── 狗狗档案管理（新增/编辑/删除）
    └── 基础设置（通知开关、数据导出）

```

---

## 3. 核心功能需求详述

### 3.1 狗狗档案管理 (Dog Profile)

* **功能描述**：用户可以创建多只狗狗的档案，并支持切换当前查看的宠物。
* **表单字段需求**：
* **头像**：支持上传本地图片
* **昵称**：文本输入（必填，限15字）
* **品种**：选择器（常见犬种如边境牧羊犬、金毛、贵宾等，支持自定义输入）
* **性别**：单选（弟弟 / 妹妹 / 已绝育公 / 已绝育母）
* **出生日期**：日期选择器（自动计算当前年龄，如：1岁2个月）
* **当前体重**：数字输入（单位：kg，支持保留两位小数）



### 3.2 疫苗记录模块 (Vaccine Tracker)

* **功能描述**：记录核心疫苗（联苗）和狂犬疫苗的接种情况。
* **核心逻辑**：
* **接种类型**：下拉选择（核心联苗-第一针、核心联苗-第二针、核心联苗-第三针、狂犬疫苗、其他疫苗）。
* **接种日期**：日期选择器（默认当天）。
* **下次接种日期**：系统根据接种类型**自动推算**（联苗间隔21天，狂犬及后续年免间隔1年），支持手动修改。
* **接种医院/备注**：文本输入（选填）。



### 3.3 驱虫记录模块 (Deworming Tracker)

* **功能描述**：区分体内、体外或内外一体驱虫，动态计算下一次驱虫时间。
* **核心逻辑**：
* **驱虫类型**：单选（体内驱虫 / 体外驱虫 / 内外一体）。
* **药品名称**：支持快捷选择常见品牌（如：犬心保、超可信、大宠爱、福来恩、拜宠清）或手动输入。
* **使用剂量**：文本输入（如：1粒、1支）。
* **使用日期**：日期选择器（默认当天）。
* **驱虫周期**：下拉选择（1个月、3个月、自定义天数）。
* **下次驱虫日期**：根据使用日期 + 周期**自动计算**。



### 3.4 首页工作台与智能提醒 (Dashboard & Reminder)

* **状态看板**：根据“下次疫苗/驱虫日期”与“当前系统日期”对比，显示三种状态：
* **安全**：距离下次事件 > 7天（绿色显示）
* **即将到期**：距离下次事件 ≤ 7天（黄色预警）
* **已超期**：当前日期 > 下次事件日期（红色高亮）


* **微信订阅消息（可选配置）**：在用户保存记录时，提示授权“疫苗到期提醒”和“驱虫到期提醒”的订阅消息。

---

## 4. 数据模型设计 (Data Model)

供 Claude Code 直接生成数据库 Schema（如微信云开发数据库、Mongoose 或 Prisma）：

### 4.1 宠物表 (`dogs`)

| 字段名 (Field) | 类型 (Type) | 必填 | 说明 |
| --- | --- | --- | --- |
| `_id` | String | 是 | 主键ID |
| `openId` | String | 是 | 用户唯一标识 |
| `name` | String | 是 | 狗狗名字 |
| `avatar` | String | 否 | 头像本地/云存储路径 |
| `breed` | String | 是 | 品种 |
| `gender` | Integer | 是 | 0: 雄性, 1: 雌性, 2: 已绝育公, 3: 已绝育母 |
| `birthday` | Date | 是 | 出生日期 |
| `weight` | Float | 否 | 当前体重 (kg) |

### 4.2 疫苗记录表 (`vaccine_records`)

| 字段名 (Field) | 类型 (Type) | 必填 | 说明 |
| --- | --- | --- | --- |
| `_id` | String | 是 | 主键ID |
| `dogId` | String | 是 | 关联的狗狗ID |
| `vaccineType` | String | 是 | 疫苗名称/类型 |
| `dateTaken` | Date | 是 | 接种日期 |
| `nextDueDate` | Date | 是 | 下次接种日期 |
| `hospital` | String | 否 | 接种医院 |
| `remark` | String | 否 | 备注信息 |

### 4.3 驱虫记录表 (`deworming_records`)

| 字段名 (Field) | 类型 (Type) | 必填 | 说明 |
| --- | --- | --- | --- |
| `_id` | String | 是 | 主键ID |
| `dogId` | String | 是 | 关联的狗狗ID |
| `type` | Integer | 是 | 0: 体内, 1: 体外, 2: 内外一体 |
| `brandName` | String | 是 | 药品名称 |
| `dateTaken` | Date | 是 | 使用日期 |
| `periodMonths` | Integer | 是 | 周期（月份数） |
| `nextDueDate` | Date | 是 | 下次驱虫日期 |

---

## 5. UI/UX 视觉与交互要求

* **设计风格**：**极简线条感 (Minimalist Line Art)**，界面干净清晰，避免复杂的色块堆叠。
* **色彩规范**：
* 主色调：健康绿（用于安全状态、保存按钮）
* 警告色：警示黄/橙（用于即将到期提醒）
* 危险色：警告红（用于已超期项目）


* **交互细节**：
* 空状态 (Empty State)：当用户尚未添加狗狗或尚无记录时，展示线条风的狗狗简笔画插图，并提示“开始开启健康守护吧”。
* 点击首页的“即将到期”卡片，应能直接跳转至对应的“添加记录”页面，并自动带入相关科目。



---

## 6. 交付与测试验收标准 (Acceptance Criteria)

1. **多宠物支持**：用户创建 2 只狗狗后，在首页切换宠物，下方的疫苗和驱虫剩余天数需实时刷新。
2. **时间逻辑正确性**：若今天（2026-05-16）添加了一次“周期为3个月”的体外驱虫，系统生成的下次驱虫日期必须准确指向 2026-08-16，且首页状态应显示为“安全（绿色）”。
3. **数据本地缓存/云端同步**：在断网状态下，基础的查看功能不受影响（如采用本地 Storage 方案），或在线上环境下云数据库读写正常。